# 🚀 5分钟快速上传指南

## ⚡ 极简步骤 - 只需要5个命令

### 第一步：创建GitHub仓库（1分钟）

1. 访问 https://github.com/new
2. Repository name: `aactp-retrospective`
3. 点击 **Create repository**
4. **复制仓库地址**（类似：`https://github.com/你的用户名/aactp-retrospective.git`）

---

### 第二步：打开终端，执行这5个命令（3分钟）

```bash
# 在项目根目录执行这些命令

# 1. 初始化Git
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Initial commit"

# 4. 设置分支名
git branch -M main

# 5. 连接并推送到GitHub（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/aactp-retrospective.git
git push -u origin main
```

---

### 第三步：等待构建（5-10分钟）

1. 打开GitHub页面
2. 点击 **Actions** 标签
3. 等待黄色圆圈变成绿色勾 ✅

---

### 第四步：下载APK（1分钟）

1. 点击完成的构建
2. 滚动到最下方 **Artifacts**
3. 点击 **AACTP-Retrospective-Debug** 下载
4. 解压ZIP文件
5. 安装APK到手机

---

## 📝 完整命令示例

假设你的GitHub用户名是 `zhangsan`：

```bash
# 在项目文件夹中打开终端，依次执行：

git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/zhangsan/aactp-retrospective.git
git push -u origin main
```

**如果出现用户名密码提示：**
- Username: 你的GitHub用户名
- Password: **不是密码！** 需要输入 Personal Access Token

---

## 🔑 如何获取Personal Access Token

如果推送时要求密码，需要Token：

1. GitHub → 右上角头像 → Settings
2. 左侧最下方 → Developer settings
3. Personal access tokens → Tokens (classic)
4. Generate new token (classic)
5. Note: `Git Push`
6. Expiration: No expiration
7. 勾选 **repo** 权限
8. Generate token
9. **复制生成的token**（只显示一次！）
10. 在命令行中用这个token作为密码

---

## ⚠️ 常见问题速查

### 问题：git: command not found
**解决：** 安装Git → https://git-scm.com/download/win

### 问题：fatal: not a git repository
**解决：** 确保在项目根目录执行命令（有.gitignore文件的目录）

### 问题：Authentication failed
**解决：** 使用Personal Access Token代替密码

### 问题：Build failed in Actions
**解决：** 
- 点击Actions → 失败的构建
- 查看红色X的错误信息
- 通常是网络问题，点击 "Re-run jobs" 重试

---

## ✅ 成功标志

上传成功后，你会看到：
- GitHub仓库页面显示你的文件
- Actions标签显示绿色 ✅
- Artifacts中有APK文件可以下载

---

## 🎉 开始吧！

**现在就去创建GitHub仓库：**
👉 https://github.com/new

有任何问题，查看 `GitHub上传指南.md` 获取详细帮助！
